FROM scratch
LABEL pl.woojciki.wojciech.kernel-param.blacklist-nouveau=rd.driver.blacklist=nouveau

COPY /tmp/lala.txt test1/test2/lala2.txt
RUN echo "nameserver 8.8.8.8" >> /etc/resolv.conf && \
    echo "nameserver 8.8.4.4" >> /etc/resolv.conf

RUN dnf -y install kernel \
    --setopt=install_weak_deps=False \
    --nodocs
